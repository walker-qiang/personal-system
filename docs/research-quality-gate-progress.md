# 投研分析质量闸门：当前进展与后续计划

更新时间：2026-08-17

## 一、专项目标

在 personal-assets 智能闭环自动化之前，先保证手动触发的 Deep Research 结果达到可用标准：

```text
数据源可追溯
    ↓
研究结果结构完整
    ↓
服务端最终质量校验
    ↓
macOS App 可读、可回读
    ↓
才允许进入自动化闭环
```

本专项不是放宽模型输出要求，而是把“研究结果能否落盘”从模型自报成功改为由 Agent 和 API 共同判定。

## 二、当前进展

### 1. 数据层基础已完成的部分

- A 股历史行情支持 WeStock / BaoStock 双源校验，并保留可信度语义。
- 删除不可稳定依赖的 Sina 网络 Provider，保留稳定 Provider 适配边界。
- A 股公告接入 CNINFO；港股接入 HKEXnews，并支持发行人 IR 官方页面回退。
- 结构化数据、原始 Provider 响应、来源元数据和官方正文证据分层保存。
- 财务报告支持多期保存、报告期规范化、原始值与计算值分离。
- 已建立 `research-data-acceptance.md`，记录真实数据集和生产门槛口径。

### 2. 研究结果质量闸门已实现的部分

`personal-agent` 已增加：

- schema v2、结果类型和必填字段校验；
- 研究日期不得早于报告期的校验；
- 指标、关键判断、风险、触发器、来源不得为空的校验；
- 来源必须包含真实 URL，并优先要求官方来源；
- 拦截 `map[...]` 等模型未展开的内部对象表示；
- 首轮结果不合格时自动发起一次结构化修复；
- 字符串数组与对象数组的归一化；
- 同一用户/session 的 Deep Research single-flight，避免重复并发；
- Runtime 失败收尾的冲突保护，避免 `operation version conflict` 覆盖真实错误；
- 客户端显示具体校验错误，不再只显示“缺少 schema v2”。

`personal-os` 已增加：

- `researchcards.ValidateForPersistence` 最终持久化校验；
- `/api/investment/analysis` 写入前的质量闸门；
- 对未来报告期、空的 highlights/thesis/antithesis/risks/triggers、缺少官方来源的结果返回 HTTP 422 并拒绝保存。

## 三、已完成的验证

- personal-agent Python 定向测试：27/27 通过。
- personal-os Go 全量测试：通过。
- macOS App Debug 构建：通过。
- API 负向回归：旧的错误研究结果会被 HTTP 422 拒绝保存。
- 真实数据层验收：6 个标的的核心财务事实已完成官方来源、报告期和单位对账，数据层生产门槛通过。

## 四、当前尚未通过的门槛

研究结果质量门槛尚未通过，原因不是服务不可用，而是模型在首轮结果被拦截后，自动修复结果仍可能返回空数组，未满足研究卡的最小内容契约。

因此当前状态是：

| 层级 | 状态 | 结论 |
| --- | --- | --- |
| 数据源与官方事实 | 通过 | 可以作为研究输入基础 |
| Agent 结构校验 | 通过 | 能识别并拒绝不完整结果 |
| API 持久化保护 | 通过 | 不完整结果不会写入研究卡 |
| 真实 Deep Research 内容质量 | 未通过 | 仍需修复模型修复闭环 |
| 自动化 personal-assets 闭环 | 未启动 | 必须等待上一个门槛通过 |

本轮真实测试产生的无效五粮液研究卡属于测试残留，不作为有效研究报告；它不应与本次代码提交混在一起处理，后续单独确认后清理。

## 五、后续计划

### Phase 1：修复质量修复闭环

1. 记录并检查首轮结果、修复提示和修复响应的结构化诊断信息。
2. 收紧修复提示：给出完整 JSON 示例、每个数组的最小数量要求和禁止空数组规则。
3. 修复失败时返回明确的质量错误，不再继续尝试或伪装成成功。
4. 保持一次修复上限，避免无界模型调用和不可控 token 成本。

### Phase 2：真实数据验收

1. 使用五粮液和腾讯分别执行真实 Deep Research。
2. 验证首轮失败能被识别，修复成功能通过，修复失败能清晰反馈。
3. 验证 API 只保存通过质量闸门的结果，并能正确回读。
4. 确认数据日期、来源 URL、指标、风险和触发器均有实际内容。

### Phase 3：产品界面验收

1. 重启完整服务栈，使用 macOS App 进行端到端验证。
2. 检查研究结果长数字、来源、风险和结论的可读性。
3. 确认新生成的研究不需要历史数据重跑即可使用新展示逻辑。
4. 清理明确确认的无效测试研究卡，但保留已保存的财报和结构化事实。

### Phase 4：生产准入与自动化

只有 Phase 1～3 全部通过后，才更新生产门槛记录，并继续 personal-assets 智能闭环：

- 手动分析结果成为自动化输入；
- 通过 API-owned write path 写入 durable source of truth；
- 保留来源、报告期、计算口径和质量状态；
- 自动化任务失败时不写入半成品研究卡。

## 六、提交边界

本次提交只包含：

- personal-agent 的研究质量校验、修复闭环、Runtime 错误保护及其相关测试；
- personal-os 的研究卡最终校验、数据源适配和相关测试/文档；
- 本文档和已有研究数据验收文档。

不包含：

- `personal-assets` 的日常未提交内容；
- SQLite、日志、缓存、运行态文件；
- 本轮真实测试产生的无效研究卡清理；
- 尚未完成的真实 Deep Research 质量验收结论。
